export function structureProducts (providors) {
  //  const providorsArray = providors.map((providor) => `
  //    `);
  //  const providorsHtml = providorsArray.join('');
  //  return providorsHtml;
  return providors;
}
